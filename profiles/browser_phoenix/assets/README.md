# Browser/Phoenix Profile Assets

This Phase 1 location will hold profile-owned browser entrypoints and static
composition assets. It contains no JavaScript copy, runtime binary, dependency,
or build output. Shared browser bridge behavior belongs to `js/blazex_runtime`.
