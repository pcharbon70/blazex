# BlazeX Charts

Provides optional chart families, chart data contracts, interaction semantics,
and accessibility fallbacks. Keeping it separate allows chart payloads and
renderer capabilities to remain opt-in.

Charts must be described semantically; concrete SVG, canvas, DOM, or native
drawing implementations belong in renderer-specific integrations.

Status: directory scaffold only; create the Mix project when its implementation
milestone begins.

