# BlazeX Data

Contains data-provider contracts and data-intensive components such as tables,
grids, lists, trees, paging, sorting, filtering, selection, and virtualization.

It must keep data access and presentation contracts host-neutral. Charting is
kept in a separate optional package so applications do not inherit its payload
or backend requirements.

Status: directory scaffold only; create the Mix project when its implementation
milestone begins.

