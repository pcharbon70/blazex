/**
 * Experimental BH-01 browser-bridge boundary probe.
 *
 * This is deliberately not a loader or component API. Runtime loading,
 * browser imports, event routing, transport, and diagnostics remain
 * unimplemented until their governed phases.
 */
export const __bh01BoundaryProbe = Object.freeze({
  scope: "browser-bridge-only",
  status: "experimental-skeleton",
});
