# BH-03 Browser Host Integration

This directory owns cross-boundary fixtures and evidence for browser runtime
discovery, startup, shared runtime ownership, independent roots, shutdown,
failure, fallback, and resource behavior.

Phase 1 activates only the empty versioned index. No lifecycle fixture,
scenario, browser result, measurement, compatibility pass, or support claim
exists yet. Later content requires separate phase authorization.

Versioned records:

- `integration-index-v0.1.0.json` — immutable Phase 1 activation index with
  planned scope and empty execution evidence.
