# BH-01 Fixture Scenarios

Disposable, versioned representative scenarios are indexed in
`../fixture-index.json`. They define governed inputs and expected outcomes for
runtime semantics, local browser behavior, the Phoenix trust boundary, and the
Phase 7 resilience/security/resource stress run. They are feasibility fixtures,
not stable BlazeX APIs or browser-support declarations.
