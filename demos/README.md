# BlazeX demos

Host-specific demonstrations live in separate subdirectories.

- [Browser](browser/README.md) — the current standalone DOM control gallery.

Run the browser gallery from the repository root:

```bash
cd demos/browser
npm start
```

Open <http://127.0.0.1:4100/>. Additional host demos can be added alongside
`browser/` as their implementations become available.
